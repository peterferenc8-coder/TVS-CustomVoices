# Build output
bin/
obj/
*.user

# Per-machine build settings (path to the local game install)
Local.props

# Release artifacts (attached to GitHub releases, not committed)
dist/

# Game assemblies must never be committed (copyrighted)
lib/
*.dll
